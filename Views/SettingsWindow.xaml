<Window x:Class="PDF_Easy_Loader.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PDF_Easy_Loader.ViewModels"
        xmlns:conv="clr-namespace:PDF_Easy_Loader.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
        Title="パスワード設定" Height="480" Width="560"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="#FFF7F7F9">

    <Window.Resources>
        <conv:EmptyStringToVisibilityConverter x:Key="EmptyToVisibility" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Foreground="#FF44444F" FontSize="12"
                   Text="登録したパスワードは、Windowsのユーザー資格情報で暗号化してアプリ専用フォルダに保存されます。登録後にパスワードを表示することはできません。" />

        <!-- 追加 -->
        <Border Grid.Row="1" Background="White" BorderBrush="#FFDDDDE5" BorderThickness="1"
                CornerRadius="6" Padding="12" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="識別名"
                           FontSize="11" Foreground="#FF6B6B76" Margin="0,0,8,4" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="パスワード"
                           FontSize="11" Foreground="#FF6B6B76" Margin="0,0,8,4" />

                <TextBox Grid.Row="1" Grid.Column="0" Padding="6,4" Margin="0,0,8,0"
                         Text="{Binding NewLabel, UpdateSourceTrigger=PropertyChanged}" />
                <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="NewPasswordInput" Padding="6,4" Margin="0,0,8,0"
                             PasswordChanged="OnPasswordChanged" />
                <Button Grid.Row="1" Grid.Column="2" Content="追加" Padding="16,4"
                        Command="{Binding AddCommand}" />
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Margin="2,8,0,0" FontSize="12" Foreground="#FF2E7D32"
                   Text="{Binding Message}"
                   Visibility="{Binding Message, Converter={StaticResource EmptyToVisibility}}" />

        <!-- 一覧 -->
        <Border Grid.Row="3" Background="White" BorderBrush="#FFDDDDE5" BorderThickness="1"
                CornerRadius="6" Margin="0,12,0,0">
            <ListBox ItemsSource="{Binding Items}" BorderThickness="0" Background="Transparent"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:PasswordItemViewModel}">
                        <DockPanel Margin="4">
                            <Button DockPanel.Dock="Right" Content="削除" Padding="12,2" FontSize="11"
                                    Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}" />
                            <StackPanel>
                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                                <TextBlock FontSize="11" Foreground="#FF6B6B76">
                                    <Run Text="最終利用: " /><Run Text="{Binding LastUsedText, Mode=OneWay}" />
                                    <Run Text="  /  パスワード: ********" />
                                </TextBlock>
                            </StackPanel>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>
